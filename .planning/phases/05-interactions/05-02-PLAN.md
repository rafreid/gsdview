# Plan 05-02: Details panel component

## Goal
Clicking a node opens a slide-out panel showing node details/metadata.

## Tasks

1. Create HTML structure for details panel (slide-out from right)
2. Add CSS for panel styling and slide animation
3. Track selected node state
4. Populate panel with node data on click
5. Add close button to dismiss panel

## Implementation

### HTML additions to index.html:
```html
<div id="details-panel" class="hidden">
  <button id="close-panel">&times;</button>
  <h2 id="panel-title"></h2>
  <div id="panel-content"></div>
</div>
```

### CSS for panel:
```css
#details-panel {
  position: fixed;
  right: 0;
  top: 0;
  width: 300px;
  height: 100%;
  background: rgba(26, 26, 46, 0.95);
  border-left: 1px solid #4ECDC4;
  padding: 20px;
  transform: translateX(100%);
  transition: transform 0.3s ease;
  z-index: 1000;
  overflow-y: auto;
}

#details-panel.visible {
  transform: translateX(0);
}

#close-panel {
  position: absolute;
  top: 10px;
  right: 10px;
  background: none;
  border: none;
  color: #fff;
  font-size: 24px;
  cursor: pointer;
}
```

### JavaScript for panel logic:
```javascript
let selectedNode = null;

function showDetailsPanel(node) {
  selectedNode = node;
  const panel = document.getElementById('details-panel');
  const title = document.getElementById('panel-title');
  const content = document.getElementById('panel-content');

  title.textContent = node.name;

  let html = `<p><strong>Type:</strong> ${node.type}</p>`;
  if (node.status) html += `<p><strong>Status:</strong> ${node.status}</p>`;
  if (node.description) html += `<p><strong>Description:</strong> ${node.description}</p>`;
  if (node.goal) html += `<p><strong>Goal:</strong> ${node.goal}</p>`;
  if (node.path) html += `<p><strong>Path:</strong> ${node.path}</p>`;
  if (node.file) html += `<p><strong>File:</strong> ${node.file}</p>`;

  content.innerHTML = html;
  panel.classList.remove('hidden');
  panel.classList.add('visible');
}

function hideDetailsPanel() {
  const panel = document.getElementById('details-panel');
  panel.classList.remove('visible');
  panel.classList.add('hidden');
  selectedNode = null;
}

document.getElementById('close-panel').addEventListener('click', hideDetailsPanel);
```

## Verification
- Click a node
- Panel slides in from right showing node details
- Click X to close panel
